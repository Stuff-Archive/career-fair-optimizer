from __future__ import unicode_literals

from django.db import models

#resume, OCI, About, industry, Bulma (alternative to bootstrap)


# class home(models.Model):
#     about = model.TextField(default = "")
#     OCI = model.BooleanField(default="")
#     industry = model.TextField(default="")
#     def __unicode__(self):
#         return self.about

# Create your models here.
